// * CHALLENGE *
// Print every character of a string 
//-------------------------------------

// declare variable
const name = 'bob';

// put every character into an array and loop over the # of characters
const arr = [name[0], name[1], name[2]];

// console.log(arr)

// loop over the string
for(i = 0; i < arr.length; i++) {
  console.log(arr[i]);
}