// * CHALLENGE *
// Print every character of a string 
//-------------------------------------

// Declare variable
const name = 'Nancy'.toLowerCase();

// loop over each character and put it inside of an array
for(i = 0; i < name.length; i++) {
  console.log(name[i]);
}
